# Инструкция: Запуск Fooocus с Realistic моделью на Yandex DataSphere

## Что такое Yandex DataSphere?

Yandex DataSphere — это облачная платформа для анализа данных и машинного обучения с поддержкой Jupyter Notebook и GPU.

**Преимущества:**
- ✅ Российский сервис
- ✅ Поддержка GPU
- ✅ Jupyter Notebook интерфейс
- ✅ Интеграция с Yandex.Cloud

## Шаг 1: Регистрация и создание проекта

1. Перейдите на https://cloud.yandex.ru/services/datasphere
2. Войдите в Yandex аккаунт (или создайте новый)
3. Создайте новый проект в DataSphere
4. Выберите тариф (проверьте актуальные условия бесплатного использования)

## Шаг 2: Создание ноутбука

1. В проекте DataSphere нажмите **"Создать"** → **"Ноутбук"**
2. Выберите конфигурацию с **GPU** (если доступно)
3. Выберите образ: **"Python 3.x"** или **"PyTorch"** (если есть)

## Шаг 3: Установка зависимостей

В первой ячейке ноутбука выполните:

```python
# Установка системных зависимостей
!apt-get update
!apt-get install -y git

# Установка Python зависимостей
!pip install pygit2==1.15.1
!pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
```

## Шаг 4: Клонирование и настройка Fooocus

```python
# Клонирование репозитория Fooocus
!cd /home/jupyter && git clone https://github.com/lllyasviel/Fooocus.git
!cd /home/jupyter/Fooocus

# Установка зависимостей Fooocus
!pip install -r requirements_versions.txt
```

## Шаг 5: Запуск Fooocus с preset realistic

```python
# Переход в директорию Fooocus
import os
os.chdir('/home/jupyter/Fooocus')

# Запуск Fooocus с preset realistic
# Используйте --listen для доступа извне (если нужно)
!python entry_with_update.py --preset realistic --listen 0.0.0.0 --port 7865
```

**Или с публичной ссылкой (если поддерживается):**

```python
!python entry_with_update.py --preset realistic --share --always-high-vram
```

## Шаг 6: Доступ к интерфейсу

После запуска вы увидите в выводе:
```
App started successful. Use the app with http://127.0.0.1:7865/
Running on public URL: https://xxxxx.gradio.live
```

**Варианты доступа:**

1. **Локальный доступ:** `http://127.0.0.1:7865` (только внутри DataSphere)
2. **Публичная ссылка:** Если использовали `--share`, получите публичную ссылку
3. **Через порт:** Если использовали `--listen`, настройте проброс портов в DataSphere

## Альтернативный способ: Запуск в фоне

Если нужно запустить в фоне и продолжить работу:

```python
import subprocess
import os

os.chdir('/home/jupyter/Fooocus')
process = subprocess.Popen([
    'python', 'entry_with_update.py', 
    '--preset', 'realistic',
    '--listen', '0.0.0.0',
    '--port', '7865'
], stdout=subprocess.PIPE, stderr=subprocess.PIPE)

print("Fooocus запущен в фоне")
print("Проверьте вывод для получения URL")
```

## Настройка для работы с моделью

Модель `realisticStockPhoto_v20` будет автоматически загружена с Hugging Face при первом запуске, так как она указана в preset `realistic.json`.

**Путь к моделям:**
- Модели сохраняются в: `/home/jupyter/Fooocus/models/checkpoints/`
- LoRA модели: `/home/jupyter/Fooocus/models/loras/`

## Полный код для одной ячейки (быстрый запуск)

```python
# Полная установка и запуск Fooocus Realistic
import os
import subprocess

# Установка зависимостей
!apt-get update -qq
!apt-get install -y -qq git
!pip install -q pygit2==1.15.1

# Клонирование Fooocus
!cd /home/jupyter && git clone -q https://github.com/lllyasviel/Fooocus.git
os.chdir('/home/jupyter/Fooocus')

# Установка зависимостей Fooocus
!pip install -q -r requirements_versions.txt

# Запуск с preset realistic
print("Запуск Fooocus с preset realistic...")
print("Модель realisticStockPhoto_v20 будет загружена автоматически")
!python entry_with_update.py --preset realistic --share --always-high-vram
```

## Важные замечания

### Ограничения:
- ⏱️ Проверьте лимиты бесплатного использования на аккаунте
- 💾 Модели загружаются каждый раз при новом запуске (если не использовать постоянное хранилище)
- 🔄 Сессии могут быть ограничены по времени

### Советы:
- ✅ Сохраняйте сгенерированные изображения в облачное хранилище Yandex
- ✅ Используйте постоянное хранилище для моделей (если доступно)
- ✅ Проверьте настройки безопасности и доступа к портам

### Если что-то пошло не так:
1. Проверьте, что выбран GPU в конфигурации ноутбука
2. Убедитесь, что все зависимости установлены
3. Проверьте логи в выводе ячейки
4. Перезапустите ядро ноутбука при необходимости

## Настройка постоянного хранилища (опционально)

Если хотите сохранить модели между сессиями:

```python
# Создание символической ссылки на постоянное хранилище
!mkdir -p /home/jupyter/persistent/Fooocus/models
!ln -s /home/jupyter/persistent/Fooocus/models /home/jupyter/Fooocus/models
```

## Проверка установки

Проверьте, что все установлено правильно:

```python
import torch
print(f"PyTorch версия: {torch.__version__}")
print(f"CUDA доступна: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"GPU: {torch.cuda.get_device_name(0)}")
```

---

**Готово!** Теперь вы можете использовать Fooocus с реалистичной моделью на Yandex DataSphere! 🎨

**Примечание:** Условия бесплатного использования могут меняться. Проверьте актуальную информацию на официальном сайте Yandex DataSphere.
